/**
 * memory heap: luu tru, cap phat bo nho
 * call stack: thuc thi code
 * web apis
 * event loop
 * callback queue
 */
/** noticing: callstack & event loop */
/**
 * làm sao để js biết là mình khôg dùng biến này nữa -> garbage collector
 * common memory leak
 * this.textLabel -> not only removeChild, have to set: this.textLabel = null; (or undefined)
 */

// scaleX
// tween gsap
